import type {
  NextFunction,
  Request,
  Response,
} from 'express';

const handle = (
  _req: Request,
  _res: Response,
  next: NextFunction,
): void => {
  // Add middleware logic here.

  next();
};

const {{ARTIFACT_CAMEL_NAME}}Middleware = {
  handle,
};

export default {{ARTIFACT_CAMEL_NAME}}Middleware;