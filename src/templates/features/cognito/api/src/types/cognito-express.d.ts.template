import type { CognitoAccessTokenPayload } from "aws-jwt-verify/jwt-model";

declare global {
  namespace Express {
    interface Request {
      auth?: CognitoAccessTokenPayload;
    }
  }
}

export {};
